name: CI/CD

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"

jobs:
  check:
    name: Check & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Check Formatting (Prettier)
        run: npx prettier "**/*.{md,yml}" --check

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache dependencies
        uses: Swatinem/rust-cache@v2

      - name: Check Formatting (Rust)
        run: cargo fmt -- --check

      - name: Check Linting (Clippy)
        run: cargo clippy -- -D warnings

      - name: Run Tests
        run: cargo test --verbose

  deploy:
    name: Deploy Release
    needs: [check]
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            name: rdu-linux-x86_64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            name: rdu-windows-x86_64.exe
          - os: macos-14
            target: aarch64-apple-darwin
            name: rdu-macos-arm64
          - os: macos-15
            target: x86_64-apple-darwin
            name: rdu-macos-x86_64

    steps:
      - uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Verify Cargo.toml version matches Tag
        shell: bash
        run: |
          TAG_VERSION=${GITHUB_REF#refs/tags/v}
          CARGO_VERSION=$(grep "^version" Cargo.toml | head -n 1 | cut -d '"' -f 2)

          if [ "$TAG_VERSION" != "$CARGO_VERSION" ]; then
            echo "::error::Version mismatch! Tag is $TAG_VERSION but Cargo.toml says $CARGO_VERSION"
            exit 1
          fi

      - name: Build Release
        run: cargo build --release --target ${{ matrix.target }}

      - name: Strip binary (Unix)
        if: runner.os != 'Windows'
        run: strip target/${{ matrix.target }}/release/rdu

      - name: Prepare Artifacts
        shell: bash
        run: |
          mkdir release
          if [ "${{ runner.os }}" = "Windows" ]; then
            cp target/${{ matrix.target }}/release/rdu.exe release/${{ matrix.name }}
          else
            cp target/${{ matrix.target }}/release/rdu release/${{ matrix.name }}
          fi

      - name: Generate Checksums
        shell: bash
        run: |
          cd release
          for file in *; do
            sha256sum "$file" > "$file.sha256"
          done

      - name: Upload Release
        uses: softprops/action-gh-release@v2
        with:
          files: release/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
